# RSP-0001 — Peter Woods Sports Reference production statistics

This Standard v1.0 package covers one primary artifact: the Peter Woods College Football at Sports-Reference.com player statistics page. It contains objective Production evidence only.

The repository owner review approved the source for objective production use, verified the ResearchSession and all five RecordedObservations independently, approved the Production EvidenceArtifact, approved the artifact-scoped package, and separately authorized its Production Population link. There are no AnalyticalObservations. No non-Production category is authorized.

The package references the existing validation-only `population-workflow:first-cohort:peter-woods`. Its 2027 target cycle and the repository's legacy 2026 Peter Woods identity classification remain an unresolved pre-existing conflict. The original Population result remains blocked; Production authorization does not infer eligibility, declaration, or draft cycle and does not mutate that result.

No source page content is copied. Run `runRSP0001Diagnostics.js` by importing and invoking `runRSP0001Diagnostics` from a development-time diagnostic command.
