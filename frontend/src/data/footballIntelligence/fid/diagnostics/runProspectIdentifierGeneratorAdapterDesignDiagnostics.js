import * as api from "../prospectIntake/index.js";
import snapshots, { canonicalGeneratorAdapterDesignContext as context } from "../prospectIntake/fixtures/prospectIdentifierGeneratorAdapterDesignSnapshots.js";
const assert = (condition, message) => { if (!condition) throw new Error(message); }; const design = api.CANONICAL_PROSPECT_IDENTIFIER_GENERATOR_ADAPTER_DESIGN; const assess = (value = design, supplied = context) => api.evaluateCanonicalProspectIdentifierGeneratorAdapterDesign(value, supplied);
const tests = [["identity", () => assert(design.designId === "CANONICAL_PROSPECT_IDENTIFIER_GENERATOR_ADAPTER_NON_PRODUCTION_DESIGN" && design.designVersion === "1.0.0", "Identity invalid.")], ["immutable", () => assert(Object.isFrozen(design) && Object.isFrozen(design.securityRules), "Mutable.")], ["no-implementation", () => assert(design.adapterIdentity.implementation === null && design.status === "DESIGNED_NOT_IMPLEMENTED", "Implemented.")], ["bindings", () => assert([design.portBinding, design.strategyBinding, design.conventionBinding, design.issuerArchitectureBinding].every((item) => item && item[Object.keys(item).find((key) => key.endsWith("Version"))] === "1.0.0"), "Binding invalid.")], ["layers", () => assert(design.supportedLayers.length === 4 && design.supportedNamespaces.length === 4, "Support invalid.")], ["non-production", () => assert(design.environmentRestrictions.productionDisabled && !design.environmentRestrictions.runtimeBindingAllowed, "Production enabled.")], ["package-boundary", () => assert(design.packageBoundary.futureImplementationFile && design.packageBoundary.prohibitedImportDirections.length === 4, "Package boundary absent.")], ["interface-design", () => assert(!design.publicInterface.implemented && design.publicInterface.futureSignature.includes("validatedDependencyBag"), "Interface implemented.")], ["dependency-bag", () => assert(design.dependencyBoundary.dependencyModel === "IMMUTABLE_VALIDATED_DEPENDENCY_BAG" && design.dependencyBoundary.validation.valid, "Dependencies invalid.")], ...["callback", "databaseClient", "rawEntropy", "persistenceAdapter"].map((key) => [`reject-${key}`, () => assert(!api.createProspectIdentifierGeneratorAdapterDependencyContract({ dependencies: design.dependencyBoundary.dependencies, [key]: key === "callback" ? () => null : {} }).validation.valid, `${key} accepted.`)]), ["plan", () => assert(api.CANONICAL_PROSPECT_IDENTIFIER_GENERATOR_ADAPTER_INVOCATION_PLAN.validation.valid && api.CANONICAL_PROSPECT_IDENTIFIER_GENERATOR_ADAPTER_INVOCATION_PLAN.steps.length === 14, "Plan invalid.")], ["plan-permissions", () => assert(api.CANONICAL_PROSPECT_IDENTIFIER_GENERATOR_ADAPTER_INVOCATION_PLAN.steps.every((step) => !step.executionPermission), "Step executable.")], ["context", () => assert(api.validateProspectIdentifierGeneratorAdapterDesignContext(context).valid, "Context invalid.")], ["context-executable-rejected", () => assert(!api.createProspectIdentifierGeneratorAdapterDesignContext({ ...context, callback: () => null }).validation.valid, "Callback accepted.")], ["ready", () => assert(assess().status === "ADAPTER_DESIGN_READY_FOR_ENTROPY_PROVIDER_CONTRACT" && assess().eligibleForEntropyProviderContract, "Not ready.")], ["encoding-deferred", () => assert(design.encodingRules.decision.startsWith("DEFERRED") && design.encodingRules.encodedExamples.length === 0, "Encoding guessed.")], ["assembly-null", () => assert(design.candidateAssemblySpecification.candidateIdentifier === null && !design.candidateAssemblySpecification.mutableFactsEncoded, "Candidate present.")], ["validation-ownership", () => assert(design.validationOwnership.issuerStructuralValidationAuthoritative && design.validationOwnership.collisionPortExternal, "Ownership invalid.")], ["result-separation", () => assert(design.resultConstructionRules.uniquenessClaimExcluded && design.resultConstructionRules.reservationClaimExcluded && design.resultConstructionRules.issuanceClaimExcluded, "Result conflated.")], ["failure-separation", () => assert(design.failureConstructionRules.collisionReservationLedgerFailuresExcluded && !design.failureConstructionRules.automaticRetry, "Failure conflated.")], ["replay-owner", () => assert(design.idempotencyRules.replayStoreOwner === "FUTURE_ISSUER_IDEMPOTENCY_BOUNDARY" && design.idempotencyRules.replayImplementation === null, "Replay owned.")], ["attempt-owner", () => assert(design.attemptRules.oneAttemptPerInvocation && design.attemptRules.issuerOwnsRetryOrchestration, "Retry owned.")], ["cancellation", () => assert(design.cancellationRules.declarativeOnly && design.cancellationRules.signalObjectProhibited, "Cancellation runtime.")], ["switching", () => assert(design.adapterSwitchingRules.silentSwitchProhibited && design.adapterSwitchingRules.issuerPolicyOrReviewRequired, "Switching silent.")], ["security", () => assert(design.securityRules.secureEntropyRequired && design.securityRules.hiddenFallbackProhibited && design.securityRules.globalMutableStateProhibited, "Security invalid.")], ["privacy", () => assert(Object.values(design.privacyRules).every((value) => value === false), "Privacy leakage.")], ["logging", () => assert(design.loggingRules.rawEntropyLoggingProhibited && design.loggingRules.fullInvocationLoggingProhibited, "Logging unsafe.")], ["tests", () => assert(design.testArchitecture.expectedCandidateValuesProhibited && design.testArchitecture.statisticalRandomnessTestProhibited, "Test generates.")], ["phases-gates", () => assert(design.implementationPhases.length === 7 && design.activationGates.length === 8, "Phases invalid.")], ...["hiddenRandomnessBlocked", "mathRandomBlocked", "timestampOnlyBlocked", "nameDerivedBlocked", "draftCycleDerivedBlocked", "adapterCollisionBlocked", "adapterReservationBlocked", "productionEnabledBlocked", "missingEntropyBoundaryBlocked", "rawEntropyLoggingBlocked", "silentSwitchBlocked"].map((key) => [`scenario-${key}`, () => assert(snapshots[key].status === "ADAPTER_DESIGN_BLOCKED", `${key} not blocked.`)]), ["batch", () => assert(snapshots.mixedBatchAssessment.assessments.length === 2 && !snapshots.mixedBatchAssessment.inputOrderSelectsDesign && !snapshots.mixedBatchAssessment.designExecuted, "Batch invalid.")], ["deterministic", () => assert(JSON.stringify(assess()) === JSON.stringify(assess()), "Nondeterministic.")], ["inputs-unchanged", () => { const before = JSON.stringify([design, context]); assess(); assert(JSON.stringify([design, context]) === before, "Mutated."); }], ["permissions", () => assert(Object.values(assess().permissions).every((value) => !value), "Permission enabled.")], ["snapshots", () => assert(Object.keys(snapshots).length === 17, "Snapshot count.")], ["cohort", () => assert(snapshots.cohortAssessment.length === 4 && snapshots.cohortAssessment[0].prospect === "Peter Woods" && snapshots.cohortAssessment.every((item) => item.adapterInvocationCount + item.entropyRequestCount + item.generatedCandidateCount === 0), "Cohort effect.")], ["no-runtime", () => assert(!JSON.stringify({ assessment: assess(), plan: api.CANONICAL_PROSPECT_IDENTIFIER_GENERATOR_ADAPTER_INVOCATION_PLAN }).match(/randomUUID|createHash|candidateIdentifier":"[^n]/i), "Runtime output.")]];
let passed = 0; for (const [name, test] of tests) { try { test(); passed += 1; console.log(`PASS ${name}`); } catch (error) { console.error(`FAIL ${name}: ${error.message}`); } } console.log(`Prospect identifier generator adapter design diagnostics: ${passed}/${tests.length}`); if (passed !== tests.length) globalThis.process.exitCode = 1;
