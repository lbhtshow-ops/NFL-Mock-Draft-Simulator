import fs from "node:fs";import path from "node:path";import crypto from "node:crypto";import {fileURLToPath} from "node:url";import record from "../persistence/deployment/FidAtomicFunctionAclFailedAttempt017c58Record.js";import analysis from "../persistence/deployment/FidAtomicFunctionAclAfterStatePredicate017c58Analysis.js";import review from "../persistence/deployment/FidAtomicFunctionAclFailedAfterState017c58Review.js";import scenarios from "../persistence/deployment/FidAtomicFunctionAclFailedAfterState017c58Scenarios.js";import staticOracle from "../persistence/deployment/FidAtomicFunctionAclFailedAfterState017c58StaticOracle.js";import evaluate from "../persistence/deployment/FidAtomicFunctionAclFailedAfterState017c58Evaluator.js";import evaluateResult from "../persistence/deployment/FidAtomicFunctionAclFailedAfterState017c58ResultEvaluator.js";import authorization from "../persistence/deployment/FidAtomicFunctionAclFailedAfterState017c58Authorization.js";
const file=path.resolve(path.dirname(fileURLToPath(import.meta.url)),"../persistence/deployment/review",review.artifact.filename),sql=fs.readFileSync(file,"utf8"),hash=crypto.createHash("sha256").update(sql).digest("hex").toUpperCase(),structural=staticOracle(sql),result=evaluate({record,analysis,review,staticReview:structural,scenarios}),failures=[];if(hash!==review.artifact.sha256)failures.push("hash");if(!result.accepted)failures.push(...result.failures);const signature=sql.match(/AS r\(([\s\S]*?)\);\s*COMMIT;/)?.[1]??"",fields=[...signature.matchAll(/\b([a-z][a-z0-9_]*)\s+(?:text|integer|bigint|boolean|jsonb)(?=,|\s*$)/g)].map(x=>x[1]),output=sql.slice(sql.indexOf("'result_identity'"),sql.indexOf(")::text,true);")),keys=[...output.matchAll(/'([a-z][a-z0-9_]*)'\s*,/g)].map(x=>x[1]);if(fields.length!==61||new Set(fields).size!==61||JSON.stringify(fields)!==JSON.stringify(keys))failures.push("visible_contract");if(JSON.stringify(structural.builderArgumentCounts)!==JSON.stringify(review.artifact.builderArgumentCounts))failures.push("builder_counts");const order=["WHEN NOT evidence_complete","WHEN NOT properties_exact OR NOT restricted_owner_exact OR NOT rollback_exact","WHEN before_exact","WHEN applied_exact","WHEN oracle_mismatch","WHEN partial_exact","ELSE 'FID_ATOMIC_FUNCTION_ACL_FAILED_REMEDIATION_STATE_INCONSISTENT_RECOVERY_REQUIRED'"];if(order.some((x,i)=>sql.indexOf(x)<0||(i&&sql.indexOf(x)<=sql.indexOf(order[i-1]))))failures.push("classification_precedence");for(const [name,changed,expected] of [["mutation",sql.replace("COMMIT;","GRANT EXECUTE ON FUNCTION x() TO y;\nCOMMIT;"),"unsafe_operation"],["lock",sql.replace("COMMIT;","LOCK TABLE x;\nCOMMIT;"),"unsafe_operation"],["rows",sql.replace("SELECT r.* FROM","SELECT r.* FROM\nSELECT r.* FROM"),"one_visible_row"],["static-metadata",sql.replace("metadata_ref:=pg_catalog.to_regclass('fid.fid_persistence_migrations')::regclass;","SELECT count(*) INTO metadata_count FROM fid.fid_persistence_migrations;"),"metadata_guard"],["client-dynamic",sql.replace("metadata_ref,'%014%'","setting_name,'%014%'"),"unsafe_dynamic_sql"],["duplicate-key",sql.replace("'result_version','17C.58.1'","'result_identity','duplicate'"),"duplicate_json_key"]]){const x=staticOracle(changed);if(x.accepted||!x.failures.includes(expected))failures.push(`adversarial:${name}`);}const invalid=evaluateResult({rows:[],target:record.target});if(invalid.accepted||!invalid.failures.includes("row_cardinality"))failures.push("result_evaluator");if(authorization.status!=="ACTIVE_UNCONSUMED"||authorization.maximumAttempts!==1||authorization.maximumExecutions!==1||authorization.reusable||authorization.retryAuthorized||authorization.artifact.sha256!==hash||authorization.remediationAuthorizationCreated!==false)failures.push("authorization");if(failures.length)throw new Error(`17C.58: ${[...new Set(failures)].join(",")}`);console.log(JSON.stringify({status:"READY_FOR_ONE_CONTROLLED_FID_ATOMIC_FUNCTION_ACL_FAILED_AFTER_STATE_READ_ONLY_RECONCILIATION_EXECUTION",hash,fields:fields.length,builderArguments:structural.builderArgumentCounts,afterStatePredicates:13,classifications:review.classifications.length,adversarialScenarios:scenarios.length,authorizationId:authorization.authorizationId,authorizationStatus:authorization.status,sqlExecuted:false,databaseConnected:false}));
