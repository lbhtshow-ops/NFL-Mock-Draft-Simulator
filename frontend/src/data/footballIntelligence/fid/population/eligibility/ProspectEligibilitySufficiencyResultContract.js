import { PROSPECT_ELIGIBILITY_RESULT_CONTRACT_VERSION, PROSPECT_ELIGIBILITY_RESULT_SCHEMA_VERSION } from "./prospectEligibilityPolicyConstants.js";

export function createProspectEligibilitySufficiencyResult(input = {}) {
  const result = { contract: "ProspectEligibilitySufficiencyResult", contractVersion: PROSPECT_ELIGIBILITY_RESULT_CONTRACT_VERSION, schemaVersion: PROSPECT_ELIGIBILITY_RESULT_SCHEMA_VERSION, resultId: input.resultId ?? null, decisionRef: input.decisionRef ?? null, proposition: input.proposition ?? null, prospectRef: input.prospectRef ?? null, draftCycleRef: input.draftCycleRef ?? null, reviewedForTime: input.reviewedForTime ?? null, eligibilityState: input.eligibilityState ?? null, basisType: input.basisType ?? null, evidencePathway: input.evidencePathway ?? "UNSUPPORTED", sourceRefs: [...(input.sourceRefs ?? [])], evidenceArtifactRefs: [...(input.evidenceArtifactRefs ?? [])], recordedObservationRefs: [...(input.recordedObservationRefs ?? [])], analyticalObservationRefs: [...(input.analyticalObservationRefs ?? [])], rulingRef: input.rulingRef ?? null, ruleRef: input.ruleRef ?? null, requiredInputs: [...(input.requiredInputs ?? [])], satisfiedInputs: [...(input.satisfiedInputs ?? [])], missingInputs: [...(input.missingInputs ?? [])], conflictingInputs: [...(input.conflictingInputs ?? [])], limitations: [...(input.limitations ?? [])], decisionRationale: input.decisionRationale ?? null, review: structuredClone(input.review ?? {}), sufficiencyState: input.sufficiencyState ?? "UNSUPPORTED", populationSufficient: input.populationSufficient === true, blockers: structuredClone(input.blockers ?? []), persistencePerformed: false, runtimeIntegrationPerformed: false, canonicalRecordCreated: false, validation: { valid: Boolean(input.resultId && input.decisionRef && input.prospectRef && input.draftCycleRef && input.reviewedForTime), errors: [], warnings: [], checkedAt: null, contractVersion: PROSPECT_ELIGIBILITY_RESULT_CONTRACT_VERSION, schemaVersion: PROSPECT_ELIGIBILITY_RESULT_SCHEMA_VERSION } };
  if (!result.validation.valid) result.validation.errors.push({ code: "INVALID_ELIGIBILITY_RESULT", path: "", message: "Result identity and scope are required." });
  return Object.freeze(result);
}
export const isProspectEligibilitySufficiencyResult = (value) => Boolean(value?.contract === "ProspectEligibilitySufficiencyResult" && value.validation?.valid);
export default Object.freeze({ createProspectEligibilitySufficiencyResult, isProspectEligibilitySufficiencyResult });
