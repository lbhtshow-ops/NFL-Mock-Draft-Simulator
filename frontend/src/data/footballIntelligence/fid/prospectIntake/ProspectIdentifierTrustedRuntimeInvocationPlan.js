import { deepFreeze } from "./ProspectIdentityDryRunRequest.js";
const stages = ["RECEIVE_HTTP_REQUEST", "VALIDATE_TRANSPORT_SHAPE", "VALIDATE_ENVIRONMENT", "APPLY_CORS_POLICY", "VERIFY_AUTHENTICATION", "RESOLVE_GOVERNED_ACTOR_REFERENCE", "VALIDATE_RUNTIME_REQUEST_CONTRACT", "VALIDATE_IDEMPOTENCY_SCOPE", "LOAD_OR_VALIDATE_ISSUANCE_AUTHORIZATION", "BUILD_ISSUER_EXECUTION_CONTEXT", "CREATE_ISSUER_PLAN", "VALIDATE_RUNTIME_DEPENDENCIES", "OBSERVE_CANCELLATION", "VALIDATE_TIMEOUT_BUDGET", "INVOKE_GENERATOR_PORT", "INVOKE_GENERATOR_ADAPTER", "REQUEST_SECURE_ENTROPY", "ENCODE_OPAQUE_COMPONENT", "ASSEMBLE_CANDIDATE", "PERFORM_ISSUER_STRUCTURAL_VALIDATION", "INVOKE_PERSISTENCE_TRANSACTION_BOUNDARY", "BUILD_GOVERNED_RUNTIME_RESULT", "RETURN_SANITIZED_HTTP_RESPONSE"];
const dependencyFor = (stage) => stage.includes("AUTHENTICATION") ? "authentication-verifier" : stage.includes("AUTHORIZATION") ? "issuance-authorization-evaluator" : stage.includes("ISSUER_PLAN") ? "issuer-planner" : stage === "INVOKE_GENERATOR_PORT" ? "generator-port" : stage === "INVOKE_GENERATOR_ADAPTER" ? "generator-adapter" : stage === "REQUEST_SECURE_ENTROPY" ? "entropy-provider+platform-secure-source" : stage === "ENCODE_OPAQUE_COMPONENT" ? "output-encoder" : stage.includes("STRUCTURAL") ? "issuer-structural-validator" : stage.includes("PERSISTENCE_TRANSACTION") ? "persistence-transaction-port" : null;
export const PROSPECT_IDENTIFIER_TRUSTED_RUNTIME_INVOCATION_PLAN = deepFreeze({ planId: "PROSPECT_IDENTIFIER_TRUSTED_RUNTIME_INVOCATION_PLAN", planVersion: "1.0.0", status: "DECLARED_NOT_EXECUTABLE", stages: stages.map((stage, index) => deepFreeze({ stepId: `runtime-boundary-step-${String(index + 1).padStart(2, "0")}`, stage, owner: stage.includes("ISSUER") || stage.includes("CANDIDATE") || stage.includes("GENERATOR") ? "EXISTING_GOVERNED_DOMAIN_OWNER" : "FID_PROSPECT_IDENTITY_TRUSTED_RUNTIME_BOUNDARY_OWNER", requiredInputs: [index === 0 ? "SANITIZED_TRANSPORT_REFERENCE" : `STEP_${String(index).padStart(2, "0")}_DECLARATION`], requiredDependency: dependencyFor(stage), expectedDeclarativeOutput: `${stage}_DECLARATION`, failureMapping: stage.includes("AUTHENTICATION") ? "AUTHENTICATION_FAILURE" : stage.includes("AUTHORIZATION") ? "AUTHORIZATION_FAILURE" : stage.includes("TRANSACTION") ? "TRANSACTION_FAILURE_OR_RECOVERY_REQUIRED" : "NORMALIZED_STAGE_FAILURE", retryClassification: "NO_AUTOMATIC_RETRY", cancellationBehavior: index < 20 ? "STOP_BEFORE_COMMIT_WHEN_OBSERVED" : "POST_COMMIT_CANNOT_UNDO_ISSUANCE", timeoutBehavior: index < 20 ? "STOP_OR_NORMALIZE_BEFORE_COMMIT" : "RECONCILE_AUTHORITATIVE_LEDGER", loggingClassification: "AUDIT_SAFE_REFERENCES_ONLY", sensitiveDataClassification: "DO_NOT_LOG", executionPermission: false })) });
export default Object.freeze({ PROSPECT_IDENTIFIER_TRUSTED_RUNTIME_INVOCATION_PLAN });
