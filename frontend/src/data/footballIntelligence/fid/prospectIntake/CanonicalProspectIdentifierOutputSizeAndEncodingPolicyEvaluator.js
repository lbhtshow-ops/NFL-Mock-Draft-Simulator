import { deepFreeze } from "./ProspectIdentityDryRunRequest.js";
import { IDENTIFIER_OUTPUT_ENCODING_POLICY_PERMISSIONS } from "./prospectIdentifierOutputEncodingPolicyConstants.js";
const probability = (n, bits) => n * (n - 1) / (2 * 2 ** bits); const encodedLength = (bytes) => Math.ceil(bytes * 8 / 6); const runtimeField = /implementation|rawEntropy|encodedEntropy|encodedExample|candidateIdentifier|randomBytes|seed|callback|client|service/i;
const inspect = (value, errors, path = "") => { if (typeof value === "function") { errors.push({ code: "EXECUTABLE_VALUE_PROHIBITED", path }); return; } if (!value || typeof value !== "object") return; Object.entries(value).forEach(([key, entry]) => { const next = path ? `${path}.${key}` : key; if (runtimeField.test(key) && entry != null && !(key === "implementation" && entry === null) && !(key === "encodedExamples" && Array.isArray(entry) && entry.length === 0)) errors.push({ code: "PROHIBITED_POLICY_VALUE", path: next }); inspect(entry, errors, next); }); };
export function createProspectIdentifierOutputEncodingPolicyReviewContext(input = {}) { const errors = []; inspect(input, errors); return deepFreeze({ contract: "PROSPECT_IDENTIFIER_OUTPUT_ENCODING_POLICY_REVIEW_CONTEXT", contractVersion: "1.0.0", contextId: input.contextId ?? null, approvedPerNamespaceScale: input.approvedPerNamespaceScale ?? null, collisionRiskCeilingExclusive: input.collisionRiskCeilingExclusive ?? null, namespaceModel: input.namespaceModel ?? null, entropyClassification: input.entropyClassification ?? null, storageConstraints: input.storageConstraints ?? [], routeConstraints: input.routeConstraints ?? [], supportedRuntimeCategories: input.supportedRuntimeCategories ?? [], crossEnvironmentRequirements: input.crossEnvironmentRequirements ?? [], currentOutputSizePolicyRef: input.currentOutputSizePolicyRef ?? null, currentEncodingPolicyRef: input.currentEncodingPolicyRef ?? null, securityReviewRef: input.securityReviewRef ?? null, architectureReviewRef: input.architectureReviewRef ?? null, productionEnabled: input.productionEnabled === true, validation: { valid: errors.length === 0, errors, warnings: [] } }); }
export function evaluateCanonicalProspectIdentifierOutputSizeAndEncodingPolicy(outputSizePolicy, encodingPolicy, context = {}) { const blockers = []; inspect(outputSizePolicy, blockers); inspect(encodingPolicy, blockers); inspect(context, blockers); const calculatedRisk = probability(context.approvedPerNamespaceScale, outputSizePolicy?.entropyBitLength); const checks = { approvedStatuses: outputSizePolicy?.status === "OUTPUT_SIZE_POLICY_APPROVED" && encodingPolicy?.status === "ENCODING_POLICY_APPROVED", exactScale: context.approvedPerNamespaceScale === 1_000_000_000 && outputSizePolicy?.approvedPerNamespaceScale === context.approvedPerNamespaceScale, exactRiskCeiling: context.collisionRiskCeilingExclusive === 1e-18 && outputSizePolicy?.collisionRiskCeilingExclusive === context.collisionRiskCeilingExclusive, bytesBitsConsistent: outputSizePolicy?.entropyByteLength * 8 === outputSizePolicy?.entropyBitLength, riskCalculationConsistent: Math.abs(outputSizePolicy?.approximateRiskAtMaximumScale - calculatedRisk) <= Number.EPSILON * Math.max(1, calculatedRisk), riskBelowCeiling: calculatedRisk < context.collisionRiskCeilingExclusive, retryExcluded: outputSizePolicy?.retryTreatment === "EXCLUDED_FROM_PRIMARY_SAFETY_CALCULATION", securitySeparated: outputSizePolicy?.securityRequirements?.collisionRiskNotSecurityStrength === true && outputSizePolicy?.securityRequirements?.cryptographicallySecureSourceStillRequired === true, encodingFamily: encodingPolicy?.encodingFamily === "RFC_4648_BASE64URL_UNPADDED", encodedLengthConsistent: encodingPolicy?.canonicalEncodedComponentLength === encodedLength(outputSizePolicy?.entropyByteLength), alphabetExact: encodingPolicy?.alphabet === "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789-_", paddingProhibited: encodingPolicy?.paddingRule === "PADDING_PROHIBITED", casePreserved: encodingPolicy?.caseRule === "CASE_SENSITIVE_PRESERVE_EXACTLY", canonicalizationComplete: encodingPolicy?.canonicalizationRules?.length === 7, delimiterCompatible: !encodingPolicy?.alphabet?.includes(encodingPolicy?.delimiter) && encodingPolicy?.delimiter === ":", providerDoesNotEncode: encodingPolicy?.providerOwnsEncoding === false, storageCompatible: encodingPolicy?.storageCompatibility?.startsWith("TEXT_AND_JSON_COMPATIBLE"), routeCompatible: encodingPolicy?.routeCompatibility?.startsWith("URL_PATH_AND_QUERY_SAFE"), runtimeHostUnresolvedAllowed: context.supportedRuntimeCategories?.includes("TRUSTED_SERVER_SIDE_RUNTIME_BOUNDARY") && !context.productionEnabled }; Object.entries(checks).filter(([, value]) => !value).forEach(([key]) => blockers.push({ code: `${key.toUpperCase()}_FAILED` })); const status = blockers.length ? "IDENTIFIER_OUTPUT_AND_ENCODING_POLICIES_BLOCKED" : "IDENTIFIER_OUTPUT_AND_ENCODING_POLICIES_APPROVED"; return deepFreeze({ contract: "PROSPECT_IDENTIFIER_OUTPUT_ENCODING_POLICY_COMPATIBILITY_ASSESSMENT", contractVersion: "1.0.0", assessmentId: context.contextId, status, checks, calculatedRisk, encodedComponentLength: encodedLength(outputSizePolicy?.entropyByteLength), fullCandidateLengths: encodingPolicy?.fullCandidateLengths ?? {}, remainingReviews: status === "IDENTIFIER_OUTPUT_AND_ENCODING_POLICIES_APPROVED" ? ["CONCRETE_TRUSTED_SERVER_RUNTIME_HOST_REVIEW"] : [], blockers, nextActions: status === "IDENTIFIER_OUTPUT_AND_ENCODING_POLICIES_APPROVED" ? ["REVIEW_CONCRETE_TRUSTED_SERVER_RUNTIME_HOST_AND_PLATFORM_SOURCE_BOUNDARY"] : ["RESOLVE_POLICY_CONFLICTS"], executionPermissions: IDENTIFIER_OUTPUT_ENCODING_POLICY_PERMISSIONS }); }
export function evaluateCanonicalProspectIdentifierOutputSizeAndEncodingPolicyBatch(outputSizePolicies = [], encodingPolicies = [], contexts = []) { const length = Math.max(outputSizePolicies.length, encodingPolicies.length, contexts.length); const assessments = Array.from({ length }, (_, index) => evaluateCanonicalProspectIdentifierOutputSizeAndEncodingPolicy(outputSizePolicies[index], encodingPolicies[index], contexts[index])); return deepFreeze({ status: assessments.some((item) => item.status === "IDENTIFIER_OUTPUT_AND_ENCODING_POLICIES_BLOCKED") ? "BATCH_IDENTIFIER_OUTPUT_AND_ENCODING_POLICIES_BLOCKED" : "BATCH_IDENTIFIER_OUTPUT_AND_ENCODING_POLICIES_APPROVED", assessments, inputOrderSelectsPolicy: false, implementationCreated: false, providerInvoked: false, encoderInvoked: false, entropyGenerated: false }); }
export default Object.freeze({ createProspectIdentifierOutputEncodingPolicyReviewContext, evaluateCanonicalProspectIdentifierOutputSizeAndEncodingPolicy, evaluateCanonicalProspectIdentifierOutputSizeAndEncodingPolicyBatch });
