import { deepFreeze } from "./ProspectIdentityDryRunRequest.js";
const blocked = new Set(["implementation", "function", "callback", "class", "client", "service", "rawentropy", "randombytes", "seed", "databasehandle", "databaseclient", "supabaseclient", "persistenceadapter", "runtimeregistry"]);
const clean = (key) => key.toLowerCase().replaceAll(/[^a-z0-9]/g, "");
const inspect = (value, path, errors) => { if (typeof value === "function") { errors.push({ code: "EXECUTABLE_DEPENDENCY_PROHIBITED", path }); return; } if (!value || typeof value !== "object") return; Object.entries(value).forEach(([key, entry]) => { const next = path ? `${path}.${key}` : key; if (blocked.has(clean(key)) && entry != null) errors.push({ code: "PROHIBITED_DEPENDENCY_FIELD", path: next }); inspect(entry, next, errors); }); };
export function createProspectIdentifierGeneratorAdapterDependencyContract(input = {}) { const errors = []; inspect(input, "", errors); const dependencies = Array.isArray(input.dependencies) ? input.dependencies : []; const requiredRoles = ["SECURE_ENTROPY_PROVIDER", "OPAQUE_OUTPUT_ENCODER", "STRATEGY_METADATA_DECLARATION", "ADAPTER_METADATA_DECLARATION"]; requiredRoles.forEach((role) => { if (!dependencies.some((item) => item.role === role)) errors.push({ code: "REQUIRED_DEPENDENCY_DECLARATION_MISSING", path: role }); }); return deepFreeze({ contract: "PROSPECT_IDENTIFIER_GENERATOR_ADAPTER_DEPENDENCY_CONTRACT", contractVersion: "1.0.0", dependencyModel: input.dependencyModel ?? "IMMUTABLE_VALIDATED_DEPENDENCY_BAG", hiddenImportsProhibited: true, environmentGlobalsProhibited: true, dependencies: dependencies.map((item) => ({ dependencyId: item.dependencyId ?? null, role: item.role ?? null, requiredContract: item.requiredContract ?? null, requiredVersion: item.requiredVersion ?? null, allowedEnvironments: item.allowedEnvironments ?? [], requiredLifecycle: item.requiredLifecycle ?? null, required: item.required !== false, runtimeOwner: item.runtimeOwner ?? null, prohibitedBehaviors: item.prohibitedBehaviors ?? [], unavailableFailureCategory: item.unavailableFailureCategory ?? null, implementation: null })), validation: { valid: errors.length === 0, errors, warnings: [] } }); }
export const validateProspectIdentifierGeneratorAdapterDependencyContract = (value) => createProspectIdentifierGeneratorAdapterDependencyContract(value).validation;
export const isProspectIdentifierGeneratorAdapterDependencyContract = (value) => Boolean(value && typeof value === "object" && validateProspectIdentifierGeneratorAdapterDependencyContract(value).valid);
export default Object.freeze({ createProspectIdentifierGeneratorAdapterDependencyContract, validateProspectIdentifierGeneratorAdapterDependencyContract, isProspectIdentifierGeneratorAdapterDependencyContract });
