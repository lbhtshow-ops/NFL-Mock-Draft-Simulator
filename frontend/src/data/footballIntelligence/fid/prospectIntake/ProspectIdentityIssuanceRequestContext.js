import { deepFreeze } from "./ProspectIdentityDryRunRequest.js";
import { PROSPECT_IDENTITY_ISSUANCE_REQUEST_CONTEXT_CONTRACT_NAME, PROSPECT_IDENTITY_ISSUANCE_REQUEST_CONTEXT_CONTRACT_VERSION } from "./prospectIdentityIssuanceRequestConstants.js";
const object = (v) => Boolean(v && typeof v === "object" && !Array.isArray(v));
const text = (v, p, e, required = false) => { if (v == null) { if (required) e.push({ code: "MISSING_REQUIRED_FIELD", path: p }); return null; } if (typeof v !== "string" || !v.trim()) { e.push({ code: "INVALID_STRING", path: p }); return null; } return v.trim(); };
export function createProspectIdentityIssuanceRequestContext(input = {}) { const x = object(input) ? input : {}; const errors = []; if (!object(input)) errors.push({ code: "INVALID_CONTEXT", path: "" }); for (const [key, value] of Object.entries(x)) if (/client|repository|database|supabase|adapter|generator|callback|service|random|clock|proposedIdentifier|canonicalRecord/i.test(key) || typeof value === "function") errors.push({ code: "PROHIBITED_CONTEXT_FIELD", path: key }); const result = { contract: PROSPECT_IDENTITY_ISSUANCE_REQUEST_CONTEXT_CONTRACT_NAME, contractVersion: PROSPECT_IDENTITY_ISSUANCE_REQUEST_CONTEXT_CONTRACT_VERSION, contextId: text(x.contextId, "contextId", errors, true), requestRef: text(x.requestRef, "requestRef", errors, true), operationRef: text(x.operationRef, "operationRef", errors, true), dryRunPlanRef: text(x.dryRunPlanRef, "dryRunPlanRef", errors, true), authorityDecisionRef: text(x.authorityDecisionRef, "authorityDecisionRef", errors, true), conventionAssessmentRef: text(x.conventionAssessmentRef, "conventionAssessmentRef", errors, true), authorityDecisionStatus: text(x.authorityDecisionStatus, "authorityDecisionStatus", errors, true), conventionAssessmentStatus: text(x.conventionAssessmentStatus, "conventionAssessmentStatus", errors, true), dryRunPlanValid: x.dryRunPlanValid === true, subjectAligned: x.subjectAligned === true, prerequisiteReferencesAligned: x.prerequisiteReferencesAligned === true, ownershipCommitted: x.ownershipCommitted === true, evidenceSatisfied: x.evidenceSatisfied === true, duplicateCandidatePresent: x.duplicateCandidatePresent === true, crossRequestDuplicate: x.crossRequestDuplicate === true, namespaceGoverned: x.namespaceGoverned === true, issuanceAuthorityCandidateRef: text(x.issuanceAuthorityCandidateRef, "issuanceAuthorityCandidateRef", errors), domainOwnerRef: text(x.domainOwnerRef, "domainOwnerRef", errors), dataStewardRef: text(x.dataStewardRef, "dataStewardRef", errors), reviewAuthorityRef: text(x.reviewAuthorityRef, "reviewAuthorityRef", errors), environment: text(x.environment, "environment", errors, true), purposePermitted: x.purposePermitted === true, lifecycleValid: x.lifecycleValid === true, lifecycleTransitionValid: x.lifecycleTransitionValid === true, idempotencyGoverned: x.idempotencyGoverned === true, policyVersionAligned: x.policyVersionAligned === true, conventionVersionAligned: x.conventionVersionAligned === true, validation: { valid: errors.length === 0, errors, warnings: [], checkedAt: null, contractVersion: PROSPECT_IDENTITY_ISSUANCE_REQUEST_CONTEXT_CONTRACT_VERSION } }; return deepFreeze(result); }
export const validateProspectIdentityIssuanceRequestContext = (v) => createProspectIdentityIssuanceRequestContext(v).validation;
export const isProspectIdentityIssuanceRequestContext = (v) => Boolean(v?.contract === PROSPECT_IDENTITY_ISSUANCE_REQUEST_CONTEXT_CONTRACT_NAME && validateProspectIdentityIssuanceRequestContext(v).valid);
export default Object.freeze({ createProspectIdentityIssuanceRequestContext, validateProspectIdentityIssuanceRequestContext, isProspectIdentityIssuanceRequestContext });
