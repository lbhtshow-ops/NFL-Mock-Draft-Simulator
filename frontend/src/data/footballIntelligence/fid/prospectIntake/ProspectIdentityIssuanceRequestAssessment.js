import { deepFreeze } from "./ProspectIdentityDryRunRequest.js";
import { CANONICAL_PROSPECT_IDENTIFIER_CONVENTION_ID, CANONICAL_PROSPECT_IDENTIFIER_CONVENTION_VERSION } from "./prospectIdentifierConventionConstants.js";
import { CANONICAL_PROSPECT_IDENTITY_AUTHORITY_POLICY_ID, CANONICAL_PROSPECT_IDENTITY_AUTHORITY_POLICY_VERSION } from "./prospectIdentityAuthorityConstants.js";
import { PROSPECT_IDENTITY_ISSUANCE_REQUEST_ASSESSMENT_CONTRACT_NAME, PROSPECT_IDENTITY_ISSUANCE_REQUEST_ASSESSMENT_CONTRACT_VERSION, PROSPECT_IDENTITY_ISSUANCE_REQUEST_PERMISSIONS, PROSPECT_IDENTITY_ISSUANCE_REQUEST_STATUSES } from "./prospectIdentityIssuanceRequestConstants.js";
export function createProspectIdentityIssuanceRequestAssessment(input = {}) { const errors = []; const required = (key) => { if (typeof input[key] !== "string" || !input[key].trim()) errors.push({ code: "MISSING_REQUIRED_FIELD", path: key }); return typeof input[key] === "string" ? input[key].trim() : null; }; const status = required("status"); if (status && !Object.values(PROSPECT_IDENTITY_ISSUANCE_REQUEST_STATUSES).includes(status)) errors.push({ code: "UNRECOGNIZED_STATUS", path: "status" }); const permissions = { ...PROSPECT_IDENTITY_ISSUANCE_REQUEST_PERMISSIONS, ...(input.permissions ?? {}) }; if (Object.values(permissions).some(Boolean)) errors.push({ code: "EXECUTION_PERMISSION_PROHIBITED", path: "permissions" }); return deepFreeze({ contract: PROSPECT_IDENTITY_ISSUANCE_REQUEST_ASSESSMENT_CONTRACT_NAME, contractVersion: PROSPECT_IDENTITY_ISSUANCE_REQUEST_ASSESSMENT_CONTRACT_VERSION, assessmentId: required("assessmentId"), requestRef: required("requestRef"), operationRef: required("operationRef"), status, reasonCodes: [...new Set(input.reasonCodes ?? [])], prerequisiteAssessment: structuredClone(input.prerequisiteAssessment ?? {}), authorityAssessment: structuredClone(input.authorityAssessment ?? {}), conventionAssessment: structuredClone(input.conventionAssessment ?? {}), identityReuseAssessment: structuredClone(input.identityReuseAssessment ?? {}), requestedEntityLayerAssessment: structuredClone(input.requestedEntityLayerAssessment ?? {}), ownershipAssessment: structuredClone(input.ownershipAssessment ?? {}), evidenceAssessment: structuredClone(input.evidenceAssessment ?? {}), duplicateAssessment: structuredClone(input.duplicateAssessment ?? {}), draftCycleAssessment: structuredClone(input.draftCycleAssessment ?? {}), lifecycleAssessment: structuredClone(input.lifecycleAssessment ?? {}), idempotencyAssessment: structuredClone(input.idempotencyAssessment ?? {}), unresolvedQuestions: [...(input.unresolvedQuestions ?? [])], blockers: structuredClone(input.blockers ?? []), requiredReviews: [...(input.requiredReviews ?? [])], requiredNextActions: [...(input.requiredNextActions ?? [])], recommendedLifecycleState: input.recommendedLifecycleState ?? null, eligibleForFutureAuthorizationReview: input.eligibleForFutureAuthorizationReview === true, policy: { policyId: CANONICAL_PROSPECT_IDENTITY_AUTHORITY_POLICY_ID, policyVersion: CANONICAL_PROSPECT_IDENTITY_AUTHORITY_POLICY_VERSION }, convention: { conventionId: CANONICAL_PROSPECT_IDENTIFIER_CONVENTION_ID, conventionVersion: CANONICAL_PROSPECT_IDENTIFIER_CONVENTION_VERSION }, permissions, executionAuthorized: false, validation: { valid: errors.length === 0, errors, warnings: [], checkedAt: null, contractVersion: PROSPECT_IDENTITY_ISSUANCE_REQUEST_ASSESSMENT_CONTRACT_VERSION } }); }
export const validateProspectIdentityIssuanceRequestAssessment = (v) => createProspectIdentityIssuanceRequestAssessment(v).validation;
export const isProspectIdentityIssuanceRequestAssessment = (v) => Boolean(v?.contract === PROSPECT_IDENTITY_ISSUANCE_REQUEST_ASSESSMENT_CONTRACT_NAME && validateProspectIdentityIssuanceRequestAssessment(v).valid);
export default Object.freeze({ createProspectIdentityIssuanceRequestAssessment, validateProspectIdentityIssuanceRequestAssessment, isProspectIdentityIssuanceRequestAssessment });
