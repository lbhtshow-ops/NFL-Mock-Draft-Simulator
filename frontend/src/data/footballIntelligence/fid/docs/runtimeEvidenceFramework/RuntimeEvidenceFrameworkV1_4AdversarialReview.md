# REF-V1.4 Adversarial Review

The review exercised package-as-conclusion, package-as-authority, source-classification promotion, overloaded empty states, chronology reordering, dangling/self/duplicate relationships, derivative and supersession cycles, transaction attribution without a transaction reference, derivative identity reuse, silent redaction, sensitive details, false custody continuity, digest-as-authenticity, package/execution identity conflation, cross-claim policy reuse, contradiction loss, uncertainty loss, duplicate derivatives as witnesses, common dependencies, circular corroboration, mutable output, and fixture authority reopening.

Controls are explicit state vocabularies, source classifications, supplied-order preservation, acyclic lineage checks, policy-scoped observations, separate local-integrity uncertainty, identity-bearing extensions, immutable outputs, and review output flags that remain false for decisions, conclusions, and authority. Operator attestation cannot satisfy a required database classification. An unobservable transaction cannot satisfy a strict transaction binding. A later observation does not erase a contradiction. A source and its derivative do not become independent witnesses.

The Sprint 17C fixture is visibly non-executable and fixture-only. Historical 17C.57-62 references are predecessor investigation context; historical authorization references are `CONSUMED_INACTIVE`; new authorization is declared required but is not created. No actual database evidence, root-cause conclusion, remediation authority, or operation success is represented.

Residual limitations: reference existence and truth cannot be authenticated locally; partial ordering is represented but no general graph scheduler is implemented; temporal references are opaque governed references rather than parsed clocks; contradiction resolution is structural and does not adjudicate truth; sufficiency policy validity is structural and still requires governance review; bounded detail uses canonical-character size rather than a transport/storage quota; duplicate JSON source keys must be rejected before JavaScript object construction; SHA-256 collision resistance and Web Crypto availability retain the V1.3 limitations; repository immutability is a future persistence/history concern, not enforced by this in-memory model.
