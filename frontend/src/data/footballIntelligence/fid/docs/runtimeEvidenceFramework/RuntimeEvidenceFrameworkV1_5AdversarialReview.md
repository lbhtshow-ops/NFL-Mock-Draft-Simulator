# REF-V1.5 Adversarial Review

The review challenged authority escalation, source authorization mutation, automatic retry, hidden second attempts, network/database/filesystem/process access, behavior-bearing payloads, fake database evidence mislabeled as actual, submission treated as receipt, transport result treated as success, digest match treated as external-byte proof, result treated as transaction proof, package completion treated as persistence, cancellation treated as external stop, missing error treated as success, chronology treated as causality, claim generalization, discarded uncertainty or contradictions, package/execution identity conflation, final-conclusion creation, environment-secret access, nondeterministic time/identity, and reuse of Sprint 17C authority.

Controls are explicit one-call orchestration, injected-only dependencies, reference-only bounded submission, recursive secret/prohibited-action rejection, immutable inputs/outputs, local modeled authorization state, fixed mandatory stop and prohibited-next-action list, explicit null operational success/conclusion, V1.4 source classifications, unresolved external/database observations, and gapped in-memory custody. The fixture names predecessor Sprints 17C.57–17C.62 only as protected history and declares new authority required.

Residual limitations: injected JavaScript code is trusted by composition and production-capable adapter admission is deferred; structural references do not authenticate their targets; cancellation checkpoints cannot prove an external continuation stopped; configured fake stage declarations can demonstrate mapping but not runtime truth; correlation references remain declared without a witness; in-memory call-count state makes one fake instance intentionally stateful for diagnostics; no persistent custody exists; Web Crypto availability and SHA-256 assumptions remain inherited V1.3 limitations; claim-policy correctness still requires governance review.
