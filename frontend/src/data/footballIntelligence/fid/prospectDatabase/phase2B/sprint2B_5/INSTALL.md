# Installation

Copy the complete replacement and additive files into the matching `frontend/` paths. No package installation, environment change, SQL, or migration is required.

Run with Vite development mode and open `/__dev/draft-results-preview`. The existing `VITE_ENABLE_FID_DRAFT_ROOM_PREVIEW=true` guard also enables the fixture Results preview in non-development builds.
