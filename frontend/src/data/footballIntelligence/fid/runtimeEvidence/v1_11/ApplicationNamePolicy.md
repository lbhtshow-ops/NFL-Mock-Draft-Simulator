# Application-name policy

Future format: `lbht-ref-v1.11-nonprod-<opaque>` where the opaque correlation token is random/bounded and not a username, secret, endpoint, or full artifact hash. The composed UTF-8 value must be validated to at most 63 bytes, recorded locally, compared with the database observation where supported, and never reused as authority. No runtime value is generated here.
