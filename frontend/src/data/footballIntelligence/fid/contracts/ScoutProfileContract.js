import {
  SCOUT_ASSIGNMENT_STATUSES, SCOUT_COVERAGE_TYPES, SCOUT_DEPARTMENT_TYPES, SCOUT_EVENT_TYPES,
  SCOUT_FOOTBALL_LEVELS, SCOUT_POSITION_GROUPS, SCOUT_PROFILE_CONFIDENCE_LEVELS,
  SCOUT_PROFILE_CONTRACT_NAME, SCOUT_PROFILE_CONTRACT_VERSION, SCOUT_PROFILE_LIFECYCLE_STATUSES,
  SCOUT_PROFILE_SCHEMA_VERSION, SCOUT_PROFILE_VERIFICATION_STATES, SCOUT_REGION_TYPES,
  SCOUT_RESPONSIBILITY_TYPES, SCOUT_ROLE_TYPES, SCOUT_STATUSES,
} from "../constants/scoutProfileConstants.js";

const REFERENCE_FIELDS = ["evaluationAssignmentRefs", "scoutingReportRefs", "observationRefs", "evidenceArtifactRefs", "prospectCoverageRefs", "playerCoverageRefs", "draftClassRefs", "educationRefs", "certificationRefs", "awardRefs", "recognitionRefs", "milestoneRefs", "relationshipRefs", "sourceRefs", "evidenceRefs"];
const PROHIBITED_EXTENSION_KEYS = new Set(["score", "grade", "rating", "ranking", "evaluation", "recommendation", "prediction", "probability", "reliability", "accuracy", "hitrate", "successrate", "evaluatorquality", "scoutquality", "reportquality", "decisionquality", "influencescore", "performancescore", "fit", "projection", "consensus", "confidencescore"]);
function isObject(value) { return Boolean(value && typeof value === "object" && !Array.isArray(value)); }
function optionalString(value) { return typeof value === "string" && value.trim() ? value.trim() : null; }
function validationShape(checkedAt = null) { return { valid: true, errors: [], warnings: [], checkedAt: optionalString(checkedAt), contractVersion: SCOUT_PROFILE_CONTRACT_VERSION, schemaVersion: SCOUT_PROFILE_SCHEMA_VERSION }; }
function add(validation, field, code, path, message) { if (!validation[field].some((entry) => entry.code === code && entry.path === path && entry.message === message)) validation[field].push({ code, path, message }); validation.valid = validation.errors.length === 0; }
function error(validation, code, path, message) { add(validation, "errors", code, path, message); }
function warning(validation, code, path, message) { add(validation, "warnings", code, path, message); }
function string(value, path, validation, required = false) { if (value == null) { if (required) error(validation, "MISSING_REQUIRED_FIELD", path, `${path} is required.`); return null; } if (typeof value !== "string" || !value.trim()) { error(validation, "INVALID_STRING", path, `${path} must be a non-empty string or null.`); return null; } return value.trim(); }
function enumValue(value, allowed, path, validation, required = false, warnUnknown = false) { if (value == null || value === "") { if (required) error(validation, "MISSING_REQUIRED_FIELD", path, `${path} is required.`); return null; } if (!Object.values(allowed).includes(value)) { error(validation, "UNRECOGNIZED_ENUM_VALUE", path, `${path} is not recognized.`); return null; } if (warnUnknown && ["UNKNOWN", "UNSPECIFIED"].includes(value)) warning(validation, "EXPLICIT_UNKNOWN_VALUE", path, `${path} is explicitly ${value}.`); return value; }
function date(value, path, validation) { const result = string(value, path, validation); if (result && Number.isNaN(Date.parse(result))) { error(validation, "INVALID_DATE", path, `${path} must be a recognizable date string or null.`); return null; } return result; }
function order(start, end, path, validation) { if (start && end && Date.parse(start) > Date.parse(end)) error(validation, "INVALID_DATE_ORDER", path, `${path} dates are not logically ordered.`); }
function boolean(value, path, validation) { if (value == null) return null; if (typeof value !== "boolean") { error(validation, "INVALID_BOOLEAN", path, `${path} must be boolean or null.`); return null; } return value; }
function version(value, path, validation) { if (value == null) return null; if ((typeof value === "string" && value.trim()) || (Number.isInteger(value) && value >= 0)) return typeof value === "string" ? value.trim() : value; error(validation, "INVALID_VERSION", path, `${path} must be a non-empty string, non-negative integer, or null.`); return null; }
function nested(value, path, validation, normalize) { if (value == null) return normalize({}); if (!isObject(value)) { error(validation, "INVALID_NESTED_STRUCTURE", path, `${path} must be an object or null.`); return normalize({}); } return normalize(value); }
function refs(value, path, validation) { if (value == null) return []; if (!Array.isArray(value)) { error(validation, "INVALID_ARRAY", path, `${path} must be an array.`); return []; } const result = []; const seen = new Set(); value.forEach((entry, index) => { if (typeof entry !== "string" || !entry.trim()) { error(validation, "INVALID_REFERENCE", `${path}[${index}]`, `${path} values must be unresolved non-empty strings.`); return; } const item = entry.trim(); if (seen.has(item)) { warning(validation, "DUPLICATE_NORMALIZED_VALUE", path, `Duplicate value removed from ${path}: ${item}.`); return; } seen.add(item); result.push(item); }); return result; }
function records(value, path, validation, idField, normalize) { if (value == null) return []; if (!Array.isArray(value)) { error(validation, "INVALID_ARRAY", path, `${path} must be an array.`); return []; } const result = []; const exact = new Set(); const ids = new Map(); value.forEach((entry, index) => { if (!isObject(entry)) { error(validation, "INVALID_RECORD_STRUCTURE", `${path}[${index}]`, `${path} entries must be objects.`); return; } const item = normalize(entry, index); const identity = JSON.stringify(item); if (item[idField] && ids.has(item[idField]) && ids.get(item[idField]) !== identity) error(validation, "DUPLICATE_RECORD_ID", `${path}[${index}].${idField}`, `${idField} must be unique within ${path}.`); if (item[idField]) ids.set(item[idField], identity); if (exact.has(identity)) { warning(validation, "DUPLICATE_NORMALIZED_OBJECT", path, `Exact duplicate removed from ${path}.`); return; } exact.add(identity); result.push(item); }); return result; }
function mergeValidation(base, extra) { const result = { ...base, errors: [...base.errors], warnings: [...base.warnings] }; extra.errors.forEach((item) => error(result, item.code, item.path, item.message)); extra.warnings.forEach((item) => warning(result, item.code, item.path, item.message)); result.valid = result.errors.length === 0; return result; }
function period(value, path, validation) { return nested(value, path, validation, (item) => { const result = { startedAt: date(item.startedAt, `${path}.startedAt`, validation), endedAt: date(item.endedAt, `${path}.endedAt`, validation), effectiveAt: date(item.effectiveAt, `${path}.effectiveAt`, validation), dateLabel: string(item.dateLabel, `${path}.dateLabel`, validation), notes: string(item.notes, `${path}.notes`, validation) }; order(result.startedAt, result.endedAt, path, validation); return result; }); }
function recordVerification(value, path, validation) { return nested(value, path, validation, (item) => ({ state: enumValue(item.state, SCOUT_PROFILE_VERIFICATION_STATES, `${path}.state`, validation), confidence: enumValue(item.confidence, SCOUT_PROFILE_CONFIDENCE_LEVELS, `${path}.confidence`, validation), verifiedBy: string(item.verifiedBy, `${path}.verifiedBy`, validation), verifiedAt: date(item.verifiedAt, `${path}.verifiedAt`, validation), limitations: string(item.limitations, `${path}.limitations`, validation), disputes: refs(item.disputes, `${path}.disputes`, validation), notes: string(item.notes, `${path}.notes`, validation) })); }
function context(item, path, validation) { return { organizationRef: string(item.organizationRef, `${path}.organizationRef`, validation), teamRef: string(item.teamRef, `${path}.teamRef`, validation), competitionRef: string(item.competitionRef, `${path}.competitionRef`, validation), seasonRefs: refs(item.seasonRefs, `${path}.seasonRefs`, validation), sourceRefs: refs(item.sourceRefs, `${path}.sourceRefs`, validation), evidenceRefs: refs(item.evidenceRefs, `${path}.evidenceRefs`, validation), verification: recordVerification(item.verification, `${path}.verification`, validation), notes: string(item.notes, `${path}.notes`, validation) }; }
function roleRecord(item, index, validation) { const path = `scoutRoles[${index}]`; return { roleId: string(item.roleId, `${path}.roleId`, validation, true), roleType: enumValue(item.roleType, SCOUT_ROLE_TYPES, `${path}.roleType`, validation, true, true), declaredTitle: string(item.declaredTitle, `${path}.declaredTitle`, validation), department: enumValue(item.department, SCOUT_DEPARTMENT_TYPES, `${path}.department`, validation, false, true), level: enumValue(item.level, SCOUT_FOOTBALL_LEVELS, `${path}.level`, validation, false, true), status: enumValue(item.status, SCOUT_ASSIGNMENT_STATUSES, `${path}.status`, validation, true, true), interim: boolean(item.interim, `${path}.interim`, validation), period: period(item.period, `${path}.period`, validation), ...context(item, path, validation) }; }
function assignmentRecord(item, index, path, validation) { const base = `${path}[${index}]`; const result = { assignmentId: string(item.assignmentId, `${base}.assignmentId`, validation, true), roleType: enumValue(item.roleType, SCOUT_ROLE_TYPES, `${base}.roleType`, validation, false, true), declaredTitle: string(item.declaredTitle, `${base}.declaredTitle`, validation), department: enumValue(item.department, SCOUT_DEPARTMENT_TYPES, `${base}.department`, validation, false, true), level: enumValue(item.level, SCOUT_FOOTBALL_LEVELS, `${base}.level`, validation, false, true), status: enumValue(item.status, SCOUT_ASSIGNMENT_STATUSES, `${base}.status`, validation, true, true), interim: boolean(item.interim, `${base}.interim`, validation), period: period(item.period, `${base}.period`, validation), ...context(item, base, validation) }; if (!result.organizationRef && !result.teamRef && !result.declaredTitle) error(validation, "ASSIGNMENT_CONTEXT_REQUIRED", base, "Assignment requires organization, team, or title context."); return result; }
function historyRecord(item, index, path, validation) { const base = `${path}[${index}]`; return { historyId: string(item.historyId, `${base}.historyId`, validation, true), label: string(item.label, `${base}.label`, validation), department: enumValue(item.department, SCOUT_DEPARTMENT_TYPES, `${base}.department`, validation, false, true), level: enumValue(item.level, SCOUT_FOOTBALL_LEVELS, `${base}.level`, validation, false, true), roleRef: string(item.roleRef, `${base}.roleRef`, validation), period: period(item.period, `${base}.period`, validation), ...context(item, base, validation) }; }
function coverageRecord(item, index, path, validation) { const base = `${path}[${index}]`; const result = { coverageId: string(item.coverageId, `${base}.coverageId`, validation, true), coverageType: enumValue(item.coverageType, SCOUT_COVERAGE_TYPES, `${base}.coverageType`, validation, true, true), regionType: enumValue(item.regionType, SCOUT_REGION_TYPES, `${base}.regionType`, validation, false, true), regionLabel: string(item.regionLabel, `${base}.regionLabel`, validation), leagueRef: string(item.leagueRef, `${base}.leagueRef`, validation), conferenceRef: string(item.conferenceRef, `${base}.conferenceRef`, validation), schoolRef: string(item.schoolRef, `${base}.schoolRef`, validation), playerCategory: string(item.playerCategory, `${base}.playerCategory`, validation), prospectCategory: string(item.prospectCategory, `${base}.prospectCategory`, validation), draftClassRef: string(item.draftClassRef, `${base}.draftClassRef`, validation), level: enumValue(item.level, SCOUT_FOOTBALL_LEVELS, `${base}.level`, validation, false, true), positionGroup: enumValue(item.positionGroup, SCOUT_POSITION_GROUPS, `${base}.positionGroup`, validation, false, true), eventRef: string(item.eventRef, `${base}.eventRef`, validation), assignmentRef: string(item.assignmentRef, `${base}.assignmentRef`, validation), scope: string(item.scope, `${base}.scope`, validation), uncertainty: string(item.uncertainty, `${base}.uncertainty`, validation), period: period(item.period, `${base}.period`, validation), ...context(item, base, validation) }; if (![result.regionType, result.regionLabel, result.competitionRef, result.leagueRef, result.conferenceRef, result.schoolRef, result.organizationRef, result.teamRef, result.playerCategory, result.prospectCategory, result.draftClassRef, result.level, result.positionGroup, result.eventRef, result.scope].some(Boolean)) error(validation, "COVERAGE_CONTEXT_REQUIRED", base, "Coverage requires a declared scope or reference."); return result; }
function responsibilityRecord(item, index, validation) { const path = `responsibilityHistory[${index}]`; return { responsibilityId: string(item.responsibilityId, `${path}.responsibilityId`, validation, true), responsibilityType: enumValue(item.responsibilityType, SCOUT_RESPONSIBILITY_TYPES, `${path}.responsibilityType`, validation, true, true), declaredLabel: string(item.declaredLabel, `${path}.declaredLabel`, validation), scope: string(item.scope, `${path}.scope`, validation), qualifiers: refs(item.qualifiers, `${path}.qualifiers`, validation), uncertainty: string(item.uncertainty, `${path}.uncertainty`, validation), status: enumValue(item.status, SCOUT_ASSIGNMENT_STATUSES, `${path}.status`, validation, true, true), period: period(item.period, `${path}.period`, validation), ...context(item, path, validation) }; }
function eventRecord(item, index, path, validation) { const base = `${path}[${index}]`; const result = { eventId: string(item.eventId, `${base}.eventId`, validation, true), eventType: enumValue(item.eventType, SCOUT_EVENT_TYPES, `${base}.eventType`, validation, true, true), title: string(item.title, `${base}.title`, validation), description: string(item.description, `${base}.description`, validation), occurredAt: date(item.occurredAt, `${base}.occurredAt`, validation), period: period(item.period, `${base}.period`, validation), assignmentRefs: refs(item.assignmentRefs, `${base}.assignmentRefs`, validation), coverageRefs: refs(item.coverageRefs, `${base}.coverageRefs`, validation), ...context(item, base, validation) }; if (!result.title && !result.description) error(validation, "EVENT_CONTEXT_REQUIRED", base, "Event requires title or description."); return result; }
function extensionValue(value, path, validation) { if (value == null || typeof value === "string" || typeof value === "boolean" || (typeof value === "number" && Number.isFinite(value))) return value; if (Array.isArray(value)) return value.map((entry, index) => extensionValue(entry, `${path}[${index}]`, validation)); if (isObject(value)) return Object.fromEntries(Object.entries(value).flatMap(([key, entry]) => { const normalized = key.toLowerCase().replaceAll(/[^a-z0-9]/g, ""); if (!key.trim()) { error(validation, "INVALID_EXTENSION_KEY", path, "Extension keys must be non-empty strings."); return []; } if (PROHIBITED_EXTENSION_KEYS.has(normalized)) { error(validation, "PROHIBITED_SCOUT_PROFILE_FIELD", `${path}.${key}`, `${key} is outside Scout Profile ownership.`); return []; } return [[key, extensionValue(entry, `${path}.${key}`, validation)]]; })); error(validation, "INVALID_EXTENSION_VALUE", path, `${path} contains an unsupported value.`); return null; }
function extensions(value, validation) { if (value == null) return {}; if (!isObject(value)) { error(validation, "INVALID_NESTED_STRUCTURE", "extensions", "extensions must be an object or null."); return {}; } return extensionValue(value, "extensions", validation); }

function normalizeScoutProfile(input, checkedAt = null) {
  const validation = validationShape(checkedAt); const profile = isObject(input) ? input : {};
  if (!isObject(input)) error(validation, "INVALID_SCOUT_PROFILE_INPUT", "", "Scout Profile input must be an object.");
  const scoutId = string(profile.scoutId, "scoutId", validation, true); const personRef = string(profile.personRef, "personRef", validation, true);
  const identity = nested(profile.identity, "identity", validation, (item) => ({ displayLabel: string(item.displayLabel, "identity.displayLabel", validation), knownProfessionalName: string(item.knownProfessionalName, "identity.knownProfessionalName", validation), profileLabels: refs(item.profileLabels, "identity.profileLabels", validation), scoutSpecificAliases: refs(item.scoutSpecificAliases, "identity.scoutSpecificAliases", validation), externalScoutIdentifiers: records(item.externalScoutIdentifiers, "identity.externalScoutIdentifiers", validation, "identifierId", (entry, index) => ({ identifierId: string(entry.identifierId, `identity.externalScoutIdentifiers[${index}].identifierId`, validation, true), namespace: string(entry.namespace, `identity.externalScoutIdentifiers[${index}].namespace`, validation, true), value: string(entry.value, `identity.externalScoutIdentifiers[${index}].value`, validation, true), sourceRefs: refs(entry.sourceRefs, `identity.externalScoutIdentifiers[${index}].sourceRefs`, validation), evidenceRefs: refs(entry.evidenceRefs, `identity.externalScoutIdentifiers[${index}].evidenceRefs`, validation), notes: string(entry.notes, `identity.externalScoutIdentifiers[${index}].notes`, validation) })) }));
  const scoutStatus = enumValue(profile.scoutStatus, SCOUT_STATUSES, "scoutStatus", validation, false, true);
  const scoutRoles = records(profile.scoutRoles, "scoutRoles", validation, "roleId", (item, index) => roleRecord(item, index, validation));
  const assignmentArrays = Object.fromEntries(["scoutAssignments", "scoutingHistory"].map((path) => [path, records(profile[path], path, validation, "assignmentId", (item, index) => assignmentRecord(item, index, path, validation))]));
  const historyArrays = Object.fromEntries(["organizationHistory", "teamHistory", "levelHistory", "departmentHistory"].map((path) => [path, records(profile[path], path, validation, "historyId", (item, index) => historyRecord(item, index, path, validation))]));
  const coverageArrays = Object.fromEntries(["coverageHistory", "regionHistory", "competitionHistory", "positionCoverageHistory"].map((path) => [path, records(profile[path], path, validation, "coverageId", (item, index) => coverageRecord(item, index, path, validation))]));
  const responsibilityHistory = records(profile.responsibilityHistory, "responsibilityHistory", validation, "responsibilityId", (item, index) => responsibilityRecord(item, index, validation));
  const eventHistory = records(profile.eventHistory, "eventHistory", validation, "eventId", (item, index) => eventRecord(item, index, "eventHistory", validation));
  const scoutTimeline = records(profile.scoutTimeline, "scoutTimeline", validation, "eventId", (item, index) => eventRecord(item, index, "scoutTimeline", validation));
  const verification = nested(profile.verification, "verification", validation, (item) => ({ state: enumValue(item.state, SCOUT_PROFILE_VERIFICATION_STATES, "verification.state", validation, true), confidence: enumValue(item.confidence, SCOUT_PROFILE_CONFIDENCE_LEVELS, "verification.confidence", validation, true), verifiedBy: string(item.verifiedBy, "verification.verifiedBy", validation), verifiedAt: date(item.verifiedAt, "verification.verifiedAt", validation), limitations: string(item.limitations, "verification.limitations", validation), disputes: refs(item.disputes, "verification.disputes", validation), notes: string(item.notes, "verification.notes", validation) }));
  const provenance = nested(profile.provenance, "provenance", validation, (item) => ({ createdBy: string(item.createdBy, "provenance.createdBy", validation), createdAt: date(item.createdAt, "provenance.createdAt", validation), updatedBy: string(item.updatedBy, "provenance.updatedBy", validation), updatedAt: date(item.updatedAt, "provenance.updatedAt", validation), originSystem: string(item.originSystem, "provenance.originSystem", validation), originRecordRef: string(item.originRecordRef, "provenance.originRecordRef", validation), notes: string(item.notes, "provenance.notes", validation) })); order(provenance.createdAt, provenance.updatedAt, "provenance", validation);
  const lifecycle = nested(profile.lifecycle, "lifecycle", validation, (item) => ({ status: enumValue(item.status, SCOUT_PROFILE_LIFECYCLE_STATUSES, "lifecycle.status", validation, true), effectiveAt: date(item.effectiveAt, "lifecycle.effectiveAt", validation), reason: string(item.reason, "lifecycle.reason", validation), notes: string(item.notes, "lifecycle.notes", validation) }));
  const versioning = nested(profile.versioning, "versioning", validation, (item) => ({ profileVersion: version(item.profileVersion, "versioning.profileVersion", validation), supersedesScoutProfileRef: string(item.supersedesScoutProfileRef, "versioning.supersedesScoutProfileRef", validation), supersededByScoutProfileRef: string(item.supersededByScoutProfileRef, "versioning.supersededByScoutProfileRef", validation), changeReason: string(item.changeReason, "versioning.changeReason", validation), notes: string(item.notes, "versioning.notes", validation) }));
  if (["VERIFIED", "VERIFIED_WITH_LIMITATIONS"].includes(verification.state)) { if (!verification.verifiedBy) error(validation, "VERIFIER_REQUIRED", "verification.verifiedBy", "Verified Scout Profiles require verifiedBy."); if (!verification.verifiedAt) error(validation, "VERIFICATION_DATE_REQUIRED", "verification.verifiedAt", "Verified Scout Profiles require verifiedAt."); }
  if (verification.state === "VERIFIED_WITH_LIMITATIONS" && !verification.limitations && !verification.notes) error(validation, "LIMITATION_CONTEXT_REQUIRED", "verification", "Verified-with-limitations requires context.");
  if (verification.state === "DISPUTED" && !verification.disputes.length && !verification.limitations && !verification.notes) error(validation, "DISPUTE_CONTEXT_REQUIRED", "verification", "Disputed verification requires context.");
  if (lifecycle.status === "DISPUTED" && !verification.disputes.length && !verification.notes && !lifecycle.notes) error(validation, "LIFECYCLE_DISPUTE_CONTEXT_REQUIRED", "lifecycle", "Disputed lifecycle requires context.");
  if (scoutId && versioning.supersedesScoutProfileRef === scoutId) error(validation, "SELF_REFERENCE", "versioning.supersedesScoutProfileRef", "Scout Profile cannot supersede itself.");
  if (scoutId && versioning.supersededByScoutProfileRef === scoutId) error(validation, "SELF_REFERENCE", "versioning.supersededByScoutProfileRef", "Scout Profile cannot be superseded by itself.");
  if (versioning.supersedesScoutProfileRef && versioning.supersedesScoutProfileRef === versioning.supersededByScoutProfileRef) error(validation, "CONFLICTING_VERSION_REFERENCES", "versioning", "Version references must identify different Scout Profiles.");
  if (lifecycle.status === "SUPERSEDED" && !versioning.supersededByScoutProfileRef) error(validation, "SUPERSEDING_PROFILE_REQUIRED", "versioning.supersededByScoutProfileRef", "SUPERSEDED requires a replacement Scout Profile reference.");
  const referenceValues = Object.fromEntries(REFERENCE_FIELDS.map((field) => [field, refs(profile[field], field, validation)])); validation.valid = validation.errors.length === 0;
  return { contract: SCOUT_PROFILE_CONTRACT_NAME, contractVersion: SCOUT_PROFILE_CONTRACT_VERSION, schemaVersion: SCOUT_PROFILE_SCHEMA_VERSION, scoutId, personRef, identity, scoutStatus, scoutRoles, ...assignmentArrays, ...historyArrays, ...coverageArrays, responsibilityHistory, ...referenceValues, eventHistory, scoutTimeline, verification, provenance, lifecycle, versioning, notes: string(profile.notes, "notes", validation), extensions: extensions(profile.extensions, validation), validation };
}

export function createScoutProfile(input = {}, { checkedAt = null } = {}) { return normalizeScoutProfile(input, checkedAt); }
export function createUnavailableScoutProfile(input = {}, { checkedAt = null } = {}) { const supplied = isObject(input) ? input : {}; const result = normalizeScoutProfile(supplied, checkedAt); const unavailable = validationShape(checkedAt); error(unavailable, "SCOUT_PROFILE_UNAVAILABLE", "", optionalString(supplied.reason) || "No usable Scout Profile is available."); return { ...result, validation: mergeValidation(result.validation, unavailable) }; }
export function validateScoutProfile(value, { checkedAt = value?.validation?.checkedAt ?? null } = {}) { return normalizeScoutProfile(value, checkedAt).validation; }
export function isScoutProfile(value) { return Boolean(isObject(value) && value.contract === SCOUT_PROFILE_CONTRACT_NAME && value.contractVersion === SCOUT_PROFILE_CONTRACT_VERSION && value.schemaVersion === SCOUT_PROFILE_SCHEMA_VERSION && validateScoutProfile(value).valid); }
export function isVerifiedScoutProfile(value) { return Boolean(isScoutProfile(value) && ["VERIFIED", "VERIFIED_WITH_LIMITATIONS"].includes(value.verification.state)); }
export function isActiveScoutProfile(value) { return Boolean(isScoutProfile(value) && value.scoutStatus === "ACTIVE"); }
export function isDisputedScoutProfile(value) { return Boolean(isScoutProfile(value) && (value.lifecycle.status === "DISPUTED" || value.verification.state === "DISPUTED")); }
export function getScoutProfilePersonRef(value) { return isScoutProfile(value) ? value.personRef : null; }

export default Object.freeze({ SCOUT_PROFILE_CONTRACT_NAME, SCOUT_PROFILE_CONTRACT_VERSION, SCOUT_PROFILE_SCHEMA_VERSION, createScoutProfile, createUnavailableScoutProfile, validateScoutProfile, isScoutProfile, isVerifiedScoutProfile, isActiveScoutProfile, isDisputedScoutProfile, getScoutProfilePersonRef });
