# DraftSelection canonical records

Approved DraftSelection revisions belong at `<record-slug>/revision-NNNN.js` and must be exported explicitly from `index.js`.

Current canonical selections:

- `draft-selection:2026:overall-29` — revision 1
