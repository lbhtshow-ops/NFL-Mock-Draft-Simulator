# Sprint 17C.30 corrected preflight result and mismatch governance review

The immutable captured row is `FID_FUNCTION_OWNER_CAPABILITY_PREFLIGHT_VISIBLE_RESULT`, version `17C.27.1`, mode `PREFLIGHT`, classification `FID_FUNCTION_OWNER_CAPABILITY_AMENDMENT_STATE_INCONSISTENT_RECOVERY_REQUIRED`, mismatch count 5, SET false, CREATE false, metadata present, migration-014 metadata count 0, evidence complete, read-only true, mutation count 0, project `ahmorpzcaapvoymiqlkv`, Primary Database, branch `main`, role `postgres`. One row was visible; no SQL error or SQLSTATE occurred. Authorization 17C.29 is consumed and retry is prohibited.

The result establishes successful SQL completion, visible read-only output, zero mutation, correct target identity, zero migration-014 metadata rows, expected-before SET/CREATE false observations, and five governed mismatches. It does not establish their identities, their cause, target drift, an oracle defect, a conclusive ACL failure, or any cleanup need. SET false and CREATE false are required before-state observations and are not standalone mismatch increments. Metadata participates only in a composite rollback-state increment.

The protected preflight has many independent increment sites, including role identity and attributes, membership options, schema controls and privileges, other-schema CREATE, table inventory/ownership and 245 table ACL units, sequence and function inventories, function security and five execute units, plus rollback metadata/object state. Multiple distinct combinations yield count five. Any static identity mapping would therefore be speculative. The amendment and migration 014 remain blocked.

Outcome B applies. The additive diagnostic `017c30_fid_function_owner_capability_mismatch_detail_read_only_diagnostic.sql`, SHA-256 `A9882BBA3AD98B4EC608FB741672ABCFCAFB1F99CC863DEE83F39C8F35061468`, preserves matrix `FID_FUNCTION_OWNER_ACL_MATRIX_017C21_V1` version `17C.21.1`: 260 object-bound checks plus one inventory invariant, 261 total governance units. It emits one Dashboard-visible row with identity, version, classification, counts, expected-before SET/CREATE observations, a deterministically ordered sanitized `mismatch_details` JSON array, target fields, read-only true, and mutation count zero. A single row remains safe under the 100-row display limit.

The diagnostic exposes only stable mismatch identifiers and governed references, expected/observed classifications, severity, and recovery classification. It exposes no raw ACL, ACL item, OID, function body, SQL definition, operational row, payload, credential, secret, URL, connection string, candidate identifier, UUID, user data, or prospect data. Optional metadata is resolved first with `to_regclass`; the only dynamic query is an aggregate SELECT whose relation identity is the catalog-resolved `regclass` and accepts no client input.

Offline pglast v8.4 with PostgreSQL 18.4 grammar parsed the complete transaction, its single visible SELECT, and its single dynamic aggregate SELECT. This is not PostgreSQL 17 proof. The diagnostic uses PostgreSQL 17-supported read-only transactions, CTEs, catalog functions/types, `aclexplode`, privilege inspection, filtered aggregates, XML query conversion, `xpath`, JSONB aggregation, and window ordering; no PostgreSQL 18-only construct was identified.

No Supabase connection, SQL execution, database operation, or authorization creation occurred. Migration 014 and the capability amendment remain unapplied, prohibited, and unauthorized.

Exact next sprint: `SPRINT_17C31_FID_FUNCTION_OWNER_CAPABILITY_MISMATCH_DETAIL_DIAGNOSTIC_INDEPENDENT_REVIEW`.
