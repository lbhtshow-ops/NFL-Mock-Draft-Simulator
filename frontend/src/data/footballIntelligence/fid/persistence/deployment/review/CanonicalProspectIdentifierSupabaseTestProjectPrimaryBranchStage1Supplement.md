# Canonical Prospect Identifier Supabase Test Project Primary-Branch Stage 1 Supplement

Status: `AUTHORIZED_DEDICATED_NON_PRODUCTION_TEST_PRIMARY_BRANCH`

This additive supplement governs only the Stage 1 classification of the exact Supabase target authorized by `CANONICAL_PROSPECT_IDENTIFIER_SUPABASE_NON_PRODUCTION_TEST_TARGET_AUTHORIZATION@1.0.0`. It does not amend historical evidence and does not authorize Stage 2 or SQL execution.

## Two-axis classification

The governed deployment environment is `DEDICATED_NON_PRODUCTION_TEST`. It remains dedicated, non-production, test-only, and production-prohibited.

The Supabase platform branch role is independently `PRODUCTION_PRIMARY_BRANCH`. The observed Dashboard label `PRODUCTION` identifies `main` as this project’s primary branch in Supabase topology. It is neither ignored nor generalized. For exact project ID `ahmorpzcaapvoymiqlkv` only, it does not override the governed LBHT non-production test classification.

## Exact Stage 1 verification

The operator must verify all of the following directly in the Dashboard without reading or recording secrets:

1. Organization is exactly `Lunch Break Hot Take`.
2. Project name is exactly `LBHT FID Persistence Test`.
3. Project ID is exactly `ahmorpzcaapvoymiqlkv`.
4. Region is exactly `us-east-1`.
5. Branch is exactly `main`.
6. The observed Supabase branch label is exactly `PRODUCTION`.
7. The governed deployment environment remains `DEDICATED_NON_PRODUCTION_TEST`.
8. The exact authorization artifact and its two classification axes are present.
9. Migration is exactly `014` and its SHA-256 is `18EC78EE6F820CE5E47DE5F71AEB2ADE413046487FFC0CA5BAC7CC2E6BF574AD`.
10. The project is active and unpaused.
11. The operator confirms the purpose is test-only, with no production traffic or production data.

Pass only when every value matches. Block for another organization, project, project ID, region, branch, platform label, migration, or hash; a paused or unknown project state; absent governed classification or authorization; real production traffic or data; or inability to confirm test-only purpose. A missing platform-label observation requires review and cannot pass.

After a Stage 1 pass, stop and report `READY_FOR_CONTROLLED_DEPLOYMENT_STAGE_2_READ_ONLY_PREFLIGHT_AUTHORIZATION_REVIEW`. Stage 2 remains prohibited until separately authorized. The read-only target preflight and controlled operator confirmation remain mandatory.
