# 017c7 controlled read-only capability preflight

Only `017c7_supabase_function_owner_deployment_capability_read_only_preflight_correction.sql`, SHA-256 `BC96C898B17BB9C55B34991E6CB164C2AB038BD16B1DDC45BC4B2F12D939FD57`, is authorized for one manual execution.

Before execution, verify project `ahmorpzcaapvoymiqlkv`, Primary Database, SQL Editor role `postgres`, and the exact artifact hash. Paste the complete file without edits and execute it once. Capture every complete sanitized result set. Stop immediately after the results are captured and return them for governance review.

This authorization does not permit `017c6`, migration 014, migration 015, migration retry, role or membership changes, SET ROLE, GRANT, REVOKE, RPC invocation, UUID or identifier generation, persistence activity, or interactive repair. A positive capability classification is evidence for a later governance decision only; it is not migration-deployment authorization.
