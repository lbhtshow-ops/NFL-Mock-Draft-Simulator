import contract from "./FidAtomicFunctionAclReconciliation017c52Contract.js";
const positive=new Set(["FID_ATOMIC_FUNCTION_ACL_RECONCILIATION_EXACT_UNCHANGED_PRE_REMEDIATION_STATE","FID_ATOMIC_FUNCTION_ACL_RECONCILIATION_OWNER_PRESERVING_REMEDIATION_ALREADY_APPLIED"]);
export function evaluateFidAtomicFunctionAclBoundedJsonb017c53Result({rows=[],target={}}={}){const failures=[];if(rows.length!==1)failures.push("row_cardinality");const row=rows[0]??{},expected=contract.fields.map(field=>field.name),actual=Object.keys(row);if(actual.length!==53||JSON.stringify(actual)!==JSON.stringify(expected))failures.push("exact_53_field_contract");if(row.result_identity!=="FID_ATOMIC_FUNCTION_ACL_FAILED_REMEDIATION_BOUNDED_JSONB_RECONCILIATION_017C52"||row.result_version!=="17C.52.1")failures.push("identity");if(!Array.isArray(row.failed_baseline_predicates)||!Number.isInteger(row.failed_baseline_predicate_count)||!Number.isInteger(row.detail_count)||row.detail_count!==row.failed_baseline_predicates.length||row.detail_count!==row.failed_baseline_predicate_count||row.detail_count_matches_failed_count!==true)failures.push("detail_reconciliation");if(positive.has(row.classification)&&row.evidence_complete!==true)failures.push("positive_incomplete");if(row.read_only!==true||row.mutation_count!==0||row.external_target_attestation_required!==true||row.overall_target_verified!==false)failures.push("governance");if(target.projectReference!=="ahmorpzcaapvoymiqlkv"||target.databaseSource!=="Primary Database"||target.sqlRole!=="postgres"||target.environment!=="DEDICATED_NON_PRODUCTION_TEST")failures.push("target");return Object.freeze({accepted:failures.length===0,failures:Object.freeze([...new Set(failures)]),classification:failures.length?"REJECTED":row.classification});}
export default evaluateFidAtomicFunctionAclBoundedJsonb017c53Result;
