const count=(text,re)=>(text.match(re)||[]).length;
function functionArgumentCounts(sql,name){const marker=`pg_catalog.${name}(`,counts=[];let start=0;while((start=sql.indexOf(marker,start))>=0){let i=start+marker.length,depth=1,quoted=false,args=1;for(;depth&&i<sql.length;i++){const c=sql[i];if(quoted){if(c==="'"&&sql[i+1]==="'"){i++;continue;}if(c==="'")quoted=false;continue;}if(c==="'"){quoted=true;continue;}if(c==="(")depth++;else if(c===")")depth--;else if(c===","&&depth===1)args++;}counts.push(args);start=i;}return counts;}
export function reviewFidAtomicFunctionAclFailedAfterState017c58(sql=""){const failures=[],clean=sql.replace(/^--.*$/gm,"");if(count(clean,/\bBEGIN\s+TRANSACTION\s+READ\s+ONLY\s*;/gi)!==1||count(clean,/\bCOMMIT\s*;/gi)!==1)failures.push("read_only_transaction");if(count(sql,/^SELECT r\.\* FROM/gm)!==1)failures.push("one_visible_row");if(!sql.includes("to_regclass('fid.fid_persistence_migrations')")||/FROM\s+fid\.fid_persistence_migrations/i.test(clean))failures.push("metadata_guard");for(const token of ["c.relkind='r'","a.attndims=1","metadata_shape_match","metadata_ref,'%014%','fid-014-identifier-issuance-transaction'","pg_catalog.pg_has_role(owner_oid","pg_catalog.pg_has_role(service_oid","pg_catalog.pg_has_role(anon_oid","pg_catalog.pg_has_role(authenticated_oid","acl_default_expansion_used","failed_after_state_predicate_count","failed_after_state_predicates","detail_count_matches_failed_count","AFTER_STATE_ORACLE_MISMATCH","external_target_attestation_required","postgres_observed_external_target_values',false","overall_target_verified',false","read_only',true","mutation_count',0"])if(!sql.includes(token))failures.push(`missing:${token}`);if(count(clean,/\b(?:GRANT|REVOKE|INSERT|UPDATE|DELETE|TRUNCATE|ALTER|CREATE|DROP|CALL|LOCK|SET\s+ROLE|CREATE\s+TEMP)\b/gi)!==0)failures.push("unsafe_operation");const dynamic=[...clean.matchAll(/^\s*EXECUTE\s+(.+)$/gmi)].map(x=>x[1]);if(dynamic.length!==1||!dynamic[0].startsWith("pg_catalog.format('SELECT count(*) FROM %s WHERE migration_id")||!sql.includes("metadata_ref,'%014%','fid-014-identifier-issuance-transaction'"))failures.push("unsafe_dynamic_sql");const builders=functionArgumentCounts(sql,"jsonb_build_object");if(builders.length!==3||Math.max(...builders)>100)failures.push("jsonb_argument_limit");const keys=[...sql.matchAll(/'([a-z][a-z0-9_]*)'\s*,/g)].map(x=>x[1]).filter(x=>!x.startsWith("after_"));const outputKeys=keys.slice(keys.indexOf("result_identity"));if(new Set(outputKeys).size!==outputKeys.length)failures.push("duplicate_json_key");return Object.freeze({accepted:failures.length===0,failures:Object.freeze([...new Set(failures)]),builderArgumentCounts:Object.freeze(builders)});}
export default reviewFidAtomicFunctionAclFailedAfterState017c58;
