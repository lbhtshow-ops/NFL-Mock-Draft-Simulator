export function evaluateFidAtomicFunctionAclReconciliation017c56({review={},staticReview={},scenarios=[]}={}){const failures=[];if(review.status!=="PASSED_WITH_ADDITIVE_SUCCESSOR"||review.successor?.readOnly!==true||review.successor?.ordinaryVisibleRows!==1)failures.push("review");if(!staticReview.accepted)failures.push(...staticReview.failures);if(scenarios.length<38||scenarios.some((x,i)=>x.ordinal!==i+1||!x.covered))failures.push("scenarios");if(review.classifications?.length!==5)failures.push("classifications");return Object.freeze({accepted:failures.length===0,failures:Object.freeze([...new Set(failures)]),decision:failures.length?"BLOCK_AUTHORIZATION":"AUTHORIZE_ONE_READ_ONLY_RECONCILIATION_EXECUTION"});}
export default evaluateFidAtomicFunctionAclReconciliation017c56;
