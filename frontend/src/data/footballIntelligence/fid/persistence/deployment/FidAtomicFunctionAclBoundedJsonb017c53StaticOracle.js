function extractCalls(sql,marker){const calls=[];let start=0;while((start=sql.indexOf(marker,start))!==-1){let i=start+marker.length,depth=1,commas=0,quoted=false,body="";for(;i<sql.length&&depth>0;i++){const c=sql[i],next=sql[i+1];if(quoted){body+=c;if(c==="'"&&next==="'"){body+=next;i++;continue;}if(c==="'")quoted=false;continue;}if(c==="'"){quoted=true;body+=c;continue;}if(c==="("){depth++;body+=c;}else if(c===")"){depth--;if(depth)body+=c;}else{if(c===","&&depth===1)commas++;body+=c;}}calls.push(Object.freeze({arguments:body.trim()?commas+1:0,body}));start=i;}return calls;}
export function inspectBuilderArguments017c53(sql=""){const calls=extractCalls(sql,"pg_catalog.jsonb_build_object(");return Object.freeze({counts:Object.freeze(calls.map(call=>call.arguments)),maximum:calls.length?Math.max(...calls.map(call=>call.arguments)):0,total:calls.reduce((sum,call)=>sum+call.arguments,0),withinLimit:calls.every(call=>call.arguments<=100)});}
const count=(text,re)=>(text.match(re)||[]).length;
export function reviewFidAtomicFunctionAclBoundedJsonb017c53(sql=""){const failures=[];const clean=sql.replace(/^--.*$/gm,""),builders=inspectBuilderArguments017c53(sql);if(JSON.stringify(builders.counts)!==JSON.stringify([52,38,16])||builders.total!==106||!builders.withinLimit)failures.push("builder_arguments");if(count(sql,/\|\| pg_catalog\.jsonb_build_object\(/g)!==2||!/\)\s*::text,true\);/.test(sql)||/\)::text\s*\|\|/.test(sql))failures.push("concat_before_text");if(count(clean,/BEGIN TRANSACTION READ ONLY;/g)!==1||count(clean,/\bCOMMIT\s*;/gi)!==1||count(sql,/^SELECT r\.\* FROM/gm)!==1)failures.push("read_only_one_row");if(/\b(?:GRANT|REVOKE|INSERT|UPDATE|DELETE|TRUNCATE|ALTER|CREATE|DROP|CALL|LOCK|FOR\s+(?:UPDATE|SHARE)|SET\s+ROLE|SET\s+SESSION\s+AUTHORIZATION|pg_advisory_|gen_random_uuid|uuid_generate)\b/i.test(clean))failures.push("unsafe_operation");for(const token of ["to_regclass('fid.fid_persistence_migrations')","c.relkind='r'","a.attnum>0 AND NOT a.attisdropped","pg_catalog.text'::pg_catalog.regtype","pg_catalog.int4'::pg_catalog.regtype","pg_catalog._text'::pg_catalog.regtype","to_regoperator('pg_catalog.~~(text,text)')","metadata_shape_match:=metadata_columns_match AND metadata_operators_match","IF metadata_shape_match THEN","EXECUTE pg_catalog.format('SELECT count(*) FROM %s","metadata_ref,'%014%','fid-014-identifier-issuance-transaction'","failed_count IS NOT NULL","detail_count IS NOT NULL","detail_count=failed_count","external_target_attestation_required","overall_target_verified","'read_only',true","'mutation_count',0"])if(!sql.includes(token))failures.push(`missing:${token}`);if(/FROM\s+fid\.fid_persistence_migrations/i.test(clean))failures.push("static_optional_relation");const precedence=["EVIDENCE_UNRESOLVED","MIGRATION_014_STATE_UNEXPECTED","FUNCTION_PROPERTY_BASELINE_DRIFT","RESTRICTED_OWNER_ATTRIBUTE_DRIFT","EXACT_UNCHANGED_PRE_REMEDIATION_STATE","OWNER_PRESERVING_REMEDIATION_ALREADY_APPLIED","PARTIAL_OR_INCONSISTENT_ACL_STATE"];let cursor=-1;for(const item of precedence){const next=sql.indexOf(item);if(next<=cursor)failures.push(`precedence:${item}`);cursor=next;}return Object.freeze({accepted:failures.length===0,failures:Object.freeze([...new Set(failures)]),builders});}
export default reviewFidAtomicFunctionAclBoundedJsonb017c53;
