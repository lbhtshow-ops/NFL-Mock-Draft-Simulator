import { FID_PERSISTENCE_MAPPING_ERROR_CATEGORIES } from "./fidPersistenceMapperConstants.js"; import { isMapperObject, mapperString, sanitizeMapperValue } from "./FidCanonicalPersistenceRowContract.js";
function factory(input, fields, required = []) { const errors = []; const value = isMapperObject(input) ? input : {}; if (!isMapperObject(input)) errors.push({ code: "INVALID_INPUT", path: "" }); const row = {}; fields.forEach((field) => { const entry = value[field]; row[field] = typeof entry === "string" ? mapperString(entry) : entry == null ? null : sanitizeMapperValue(entry, field, errors); if (required.includes(field) && row[field] == null) errors.push({ code: "REQUIRED_FIELD", path: field }); }); return Object.freeze({ ...row, canonical_fid_data: false, generated: false, persisted: false, effect_occurred: false, validation: { valid: errors.length === 0, errors, warnings: [] } }); } const define = (creator) => ({ validate: (value) => creator(value).validation, guard: (value) => isMapperObject(value) && creator(value).validation.valid });
export const createFidIdempotencyRow = (input = {}) => factory(input, ["idempotency_key", "request_id", "batch_id", "operation_id", "request_hash", "result_status", "persistence_id", "canonical_record_id", "record_revision", "effect_receipt_id", "first_seen_at", "completed_at", "outcome_indeterminate", "result_payload"], ["idempotency_key"]); const idem = define(createFidIdempotencyRow); export const validateFidIdempotencyRow = idem.validate; export const isFidIdempotencyRow = idem.guard;
export const createFidPersistenceBatchRow = (input = {}) => factory(input, ["batch_id", "request_id", "expected_operation_count", "requested_atomicity", "actor_ref", "source_refs", "lifecycle", "notes", "started_at", "completed_at", "committed_operation_count", "failed_operation_count", "achieved_atomicity", "commit_state", "rollback_state"], ["batch_id"]); const batch = define(createFidPersistenceBatchRow); export const validateFidPersistenceBatchRow = batch.validate; export const isFidPersistenceBatchRow = batch.guard;
export const createFidPersistenceBatchOperationRow = (input = {}) => factory(input, ["batch_id", "operation_id", "operation_index", "action", "persistence_id", "canonical_record_id", "record_revision", "preconditions", "expected_result_ref"], ["batch_id", "operation_id"]); const operation = define(createFidPersistenceBatchOperationRow); export const validateFidPersistenceBatchOperationRow = operation.validate; export const isFidPersistenceBatchOperationRow = operation.guard;
export const createFidPersistenceEffectReceiptRow = (input = {}) => factory(input, ["receipt_id", "request_ref", "batch_ref", "operation_ref", "repository_ref", "effect_type", "persistence_id", "canonical_record_id", "record_revision", "predecessor_persistence_id", "committed", "durable", "commit_state", "atomicity_state", "occurred_at", "actor_ref", "verification", "audit_refs", "notes"], ["receipt_id"]); const receipt = define(createFidPersistenceEffectReceiptRow); export const validateFidPersistenceEffectReceiptRow = receipt.validate; export const isFidPersistenceEffectReceiptRow = receipt.guard;
export const createFidPersistenceAuditEventRow = (input = {}) => factory(input, ["event_id", "event_type", "request_ref", "batch_ref", "operation_ref", "repository_ref", "actor_ref", "occurred_at", "category", "severity", "message", "details", "source_refs", "notes"], ["event_id", "event_type"]); const audit = define(createFidPersistenceAuditEventRow); export const validateFidPersistenceAuditEventRow = audit.validate; export const isFidPersistenceAuditEventRow = audit.guard;
export function createFidDatabaseFailureInput(input = {}) { const row = factory(input, ["code", "category", "constraint_name", "table_name", "column_name", "request_ref", "operation_ref", "persistence_id", "record_id", "revision", "predecessor_ref", "recoverable", "outcome_indeterminate", "details"], ["code"]); const errors = [...row.validation.errors]; if (row.category && !Object.values(FID_PERSISTENCE_MAPPING_ERROR_CATEGORIES).includes(row.category)) errors.push({ code: "UNKNOWN_FAILURE_CATEGORY", path: "category" }); return Object.freeze({ ...row, validation: { ...row.validation, valid: errors.length === 0, errors } }); } const failure = define(createFidDatabaseFailureInput); export const validateFidDatabaseFailureInput = failure.validate; export const isFidDatabaseFailureInput = failure.guard;
export const mapIdempotencyMetadataToFidRow = createFidIdempotencyRow; export const mapBatchMetadataToFidRow = createFidPersistenceBatchRow; export const mapEffectReceiptToFidRow = createFidPersistenceEffectReceiptRow; export const mapAuditEventToFidRow = createFidPersistenceAuditEventRow; export const normalizeDatabaseFailureForFidMapping = createFidDatabaseFailureInput;
export default Object.freeze({ createFidIdempotencyRow, validateFidIdempotencyRow, isFidIdempotencyRow, createFidPersistenceBatchRow, validateFidPersistenceBatchRow, isFidPersistenceBatchRow, createFidPersistenceBatchOperationRow, validateFidPersistenceBatchOperationRow, isFidPersistenceBatchOperationRow, createFidPersistenceEffectReceiptRow, validateFidPersistenceEffectReceiptRow, isFidPersistenceEffectReceiptRow, createFidPersistenceAuditEventRow, validateFidPersistenceAuditEventRow, isFidPersistenceAuditEventRow, createFidDatabaseFailureInput, validateFidDatabaseFailureInput, isFidDatabaseFailureInput, mapIdempotencyMetadataToFidRow, mapBatchMetadataToFidRow, mapEffectReceiptToFidRow, mapAuditEventToFidRow, normalizeDatabaseFailureForFidMapping });
