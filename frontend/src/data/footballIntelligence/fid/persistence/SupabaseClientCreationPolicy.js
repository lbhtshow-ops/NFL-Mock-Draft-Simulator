import { SUPABASE_CLIENT_FACTORY_SUPPORTED_ENVIRONMENTS, SUPABASE_CLIENT_FACTORY_SUPPORTED_PURPOSES } from "./supabaseClientFactoryConstants.js";
export function validateSupabaseClientCreationPolicy({ runtimeConfiguration, constructionRequest, runtimeTarget, resolvedValues } = {}) { const errors = []; let status = "BLOCKED"; if (!runtimeConfiguration?.validation?.valid) { errors.push({ code: "INVALID_RUNTIME_CONFIGURATION" }); status = "INVALID_CONFIGURATION"; } if (!constructionRequest?.validation?.valid) { errors.push({ code: "INVALID_CONSTRUCTION_REQUEST" }); status = "INVALID_REQUEST"; } const purpose = constructionRequest?.clientPurpose; const environment = constructionRequest?.environment; if (!SUPABASE_CLIENT_FACTORY_SUPPORTED_PURPOSES.includes(purpose)) { errors.push({ code: "UNSUPPORTED_PURPOSE" }); status = "UNSUPPORTED_PURPOSE"; } if (!SUPABASE_CLIENT_FACTORY_SUPPORTED_ENVIRONMENTS.includes(environment)) { errors.push({ code: "UNSUPPORTED_ENVIRONMENT" }); status = "UNSUPPORTED_ENVIRONMENT"; } const credentialType = constructionRequest?.credentialReference?.credentialType; if (runtimeTarget === "BROWSER" && (purpose === "SERVER_PRIVILEGED" || credentialType === "SERVICE_ROLE_KEY")) { errors.push({ code: "BROWSER_PRIVILEGED_PROHIBITED" }); status = "REJECTED_BROWSER_PRIVILEGED"; } if (runtimeTarget === "BROWSER" && purpose === "SERVER_STANDARD") { errors.push({ code: "SERVER_PURPOSE_BROWSER_MISMATCH" }); status = "REJECTED_SERVER_MISMATCH"; } if (runtimeTarget === "SERVER" && ["PUBLIC_READ_ONLY", "AUTHENTICATED_USER"].includes(purpose) && credentialType === "SERVICE_ROLE_KEY") { errors.push({ code: "PUBLIC_PURPOSE_SERVICE_ROLE_MISMATCH" }); status = "REJECTED_SERVER_MISMATCH"; } if (!resolvedValues?.projectUrl || !resolvedValues?.credential) { errors.push({ code: "MISSING_RESOLVED_CONSTRUCTION_VALUES" }); if (status === "BLOCKED") status = "BLOCKED"; } if (resolvedValues?.projectUrl && !/^https:\/\//i.test(resolvedValues.projectUrl)) { errors.push({ code: "INVALID_PROJECT_URL" }); status = "INVALID_REQUEST"; } return Object.freeze({ valid: errors.length === 0, status: errors.length ? status : "CREATED", errors, purpose, environment, credentialType, runtimeTarget, connectionAttempted: false, productionApproved: false }); }
export default Object.freeze({ validateSupabaseClientCreationPolicy });
